<script lang="ts">
import { useElementSize } from "$lib/hooks/useElementSize";

let ref: HTMLElement | null = null;

$: size = useElementSize(ref);

// use $size.width/$size.height like this

</script>

<div bind:this={ref} />